# Contributing

Thanks for looking. This is a small project with a small surface, so the bar
for a change is mostly: does it make the thing simpler to run, or fix something
that is wrong?

## Getting set up

See [Getting started](README.md#getting-started) for prerequisites and the two
ways to run it. In short: Node 24 or newer, a Unix-like system, `npm install`.

Everything runs from the repository root, and Turborepo builds whatever a task
depends on first:

```bash
npm run lint          # Biome — formatting and lint in one pass
npm run format        # write the formatting fixes
npm run build         # every workspace
npm run check-types   # every workspace (builds first — see below)
npm run preview       # build and run the Worker locally
npm test              # the test suite
```

`public/pdfjs/` appearing in the app is expected and gitignored: the `assets`
task copies pdf.js's worker, CMaps, fonts and wasm out of `pdfjs-dist`, because
pdf.js fetches those by URL and no bundler sees the request. Everything that
serves the app depends on that task, so there is nothing to run by hand.

`check-types` builds before it typechecks, because the app's route types are
generated by `next build`. That is why it is slower than you might expect.

CI runs lint, build and typecheck on Node 24, separately bundles the Worker — a
change can pass `next build` and still fail to bundle for workerd — and runs the
tests.

## Tests

`npm test` runs them all — it fans out through Turborepo, so each package runs
its own and a change to one re-runs only what depends on it. `npm test -w
@bookshelf/core` runs a single package's.

Tests live beside the code they exercise, as `test/*.test.ts` in each package
and in the app. Node runs them from source by stripping the types; they are
type-checked too, by the package's own `check-types`.

Everything is generated. `@bookshelf/fixtures` writes archives, EPUBs — real
ZIP, real PNG cover, real Dublin Core — and PDFs, one per combination of the
ways that format can record the same thing. Nothing reaches the network, needs
a book to hand, or depends on `cwebp` or `pdftoppm` being installed. A fixture
that changes every time it is generated cannot be compared, so they are
deterministic too. `npm run demo` builds its shelf from the same generators,
which is why they are a package rather than a test-only directory.

The app's fakes are its own, in `apps/bookshelf/test/lib/storage.ts`: a library
in memory that counts what was asked of it and can be told to be unwritable, or
to fail, or to fail only some operations. That last one matters more than it
sounds — a read that fails while writes still work is how a rewritten file loses
everything it could not read first, and a fixture that breaks everything at once
cannot tell you whether that is handled.

Two things there are workarounds rather than choices. Node does not know the
app's `@/` path alias, so `apps/bookshelf/test/lib/alias.mjs` registers a
resolver and the app's `test` script loads it with `--import`; the alias is
resolved by Next at build time, and `node --test` does no building. And the app
excludes `test` from `tsconfig.json` so `next build` does not compile the tests
into the bundle, which is what `tsconfig.test.json` is for.

What is still out of reach is anything above the services: routes, pages, and
the composition root, which need `next/headers` and a Cloudflare context. The
filesystem quickstart is what covers those.

When adding a test, prefer pinning a promise over an implementation. Several
here assert something the source explains at length — that an entry costs one
ranged read, that `--force` cannot take everyone's bookmarks with it, that a
range a provider declined comes back as a whole object. Those are the ones worth
having, because they are the ones a future change can quietly break.

## What lives where

```
apps/bookshelf       the app — a Next.js Worker, or a Node server
packages/core        the library format, the ZIP reader, the provider contract
packages/provider-r2 Cloudflare R2
packages/provider-fs a directory on disk
packages/sync        the CLI that builds a library and publishes it
packages/fixtures    generated books, for tests and for the demo shelf
```

The app talks to interfaces and never to a specific provider. If you find
yourself importing `@bookshelf/provider-*` anywhere but
`apps/bookshelf/src/services/container.ts`, something has gone sideways.

## Adding a storage provider

Providers are the extension point, and they do not have to live here — a
package published by anyone can be installed and named in
`bookshelf.config.json`. If you want one shipped in this repository, copy
`packages/provider-fs` and read `packages/core/src/provider.ts` first.

Five things a provider has to get right:

**Name entry points for the runtime they need, not the face they implement.**
`/worker` for workerd, `/node` for Node. R2 has both because the app reads it
from a Worker binding and the CLI writes it through wrangler; the filesystem
has only `/node`, because both of its halves need a filesystem. Node-only
imports must never be reachable from a `/worker` entry point — CI bundles the
Worker partly to catch that.

**Declare only capabilities you have.** `create`, `list` and `removeAll` are
optional because storage APIs genuinely differ. Leave them undefined rather
than implementing something that half works: the sync tool asks with
`capabilitiesOf()` and degrades honestly, and `--force` tells the user which
guarantee it actually gave them.

**Keep `.bookshelf/` out of enumeration.** It holds profiles and reading
positions — the only things in a library the app wrote rather than the sync
tool. `list` reports what the sync tool may remove, so anything that reports
state keys will have `--force` delete everyone's bookmarks. `isStateKey()` in
`@bookshelf/core` is there for this.

**Serve a range or decline it, but say which.** `read` takes an optional
`range`. Honour it and set `range` on what you return, naming the bytes the body
holds; ignore it and return the whole object with nothing set. The caller reads
that rather than its own request, so declining costs a complete response instead
of a partial one mislabelled as complete — see
[serving part of an object](docs/providers/README.md#serving-part-of-an-object).

**Treat keys as untrusted.** They arrive from URLs. Resolving one that escapes
the library must fail, not be tidied up; `resolveKey` in
`packages/provider-fs/src/node.ts` is the reference.

## Commit messages

The seven rules from <https://chris.beams.io/git-commit>:

1. Separate subject from body with a blank line.
2. Limit the subject to about 50 characters.
3. Capitalise the subject.
4. No full stop at the end of the subject.
5. Imperative mood — the subject completes "If applied, this commit will ___".
   `Add reading dispatcher`, not `Added` or `Adds` or `Fixed`.
6. Wrap the body at 72 characters.
7. The body explains **what and why**, not how; the diff shows how. Add one
   only when the change needs context — motivation, a side effect, a trade-off
   — and skip it for trivial changes.

Please don't add tool attribution trailers: no `Co-Authored-By` for an
assistant, no "generated with" lines, no emoji signatures. Write the message as
you would have written it yourself. Using an assistant to write the code is
fine; the commit log is for explaining the change, not the process.

## Pull requests

Small and focused beats large and complete. If a change does two things, two
pull requests are easier to review and easier to revert.

Say what you verified and how. `npm test` passing is the baseline; "ran the
filesystem quickstart, opened a book, switched profiles" is what says the change
works, and much of this project is still only reachable that way.

`main` takes changes through pull requests only, and every CI job has to pass
before one can merge. Merges are squashed, so the branch can have as many
commits as it needs — write the final message on the pull request.

## Reporting a security issue

Please don't open a public issue for anything exploitable — see
[SECURITY.md](SECURITY.md), which explains where to send it and what does not
count (the missing authentication, chiefly).
